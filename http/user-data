#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: de
    variant: de
  network:
    network:
      version: 2
      ethernets:
        ens192:
          dhcp4: true
  storage:
    layout:
      name: lvm
      match:
        path: /dev/sda
  ssh:
    install-server: yes
  user-data:
    disable_root: false
    users:
      - name: ansible
        passwd: $5$X112WxpLAeWkXIEj$3fPK/smH/7a9AJGsoe9PfnwNkR5e4PSgp7WsbtwxJFB
        shell: /bin/bash
        lock_passwd: false
        uid: "1001"
    hostname: ubuntu-template
  late-commands:
    - 'sed -i "s/dhcp4: true/&\n      dhcp-identifier: mac/" /target/etc/netplan/00-installer-config.yaml'
    - echo 'ansible ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/ansible
